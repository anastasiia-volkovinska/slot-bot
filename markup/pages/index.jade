doctype html
html(lang="ru")

    include ../components/head/head

    head
        +head(head.defaults)
    body

        section.Form
            form.Form__form
                input.Form__name(placeholder="name")
                input.Form__gameID(placeholder="gameID")
                input.Form__userID(placeholder="userID")
                input.Form__casinoID(placeholder="casinoID")
                input.Form__submit(type="submit")

        script(src='%=static=%js/separate-js/svg4everybody.min.js')
        script svg4everybody();

        script(src='%=static=%js/main%=hash=%%=min=%.js')
